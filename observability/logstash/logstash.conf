input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  # Add filters if needed
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "student-management-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
