# Production profile: enable Flyway migrations
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# In production, ensure you set a proper datasource via environment variables
# and avoid `spring.jpa.hibernate.ddl-auto` (we set it to none by default).
